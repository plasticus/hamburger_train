name: hamburger_train
description: A chill burger tycoon game. 🍔🚂
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2         # State management
  confetti: ^0.7.0          # Celebration moments 🎉
  shared_preferences: ^2.3.2 # Save game state locally

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
